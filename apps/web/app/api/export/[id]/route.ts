export async function GET(_request:Request,{params}:{params:Promise<{id:string}>}){const {id}=await params;const base=process.env.API_URL??"http://127.0.0.1:8000";const token=process.env.LOCAL_API_TOKEN;const response=await fetch(`${base}/api/v1/analyses/${encodeURIComponent(id)}/export?format=markdown`,{headers:token?{authorization:`Bearer ${token}`}:{},cache:"no-store"});return new Response(await response.text(),{status:response.status,headers:{"content-type":response.headers.get("content-type")??"text/markdown","content-disposition":`attachment; filename="intentledger-${id}.md"`}})}
