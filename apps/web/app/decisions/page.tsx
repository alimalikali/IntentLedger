export default function Page(){return <section className="page"><div className="eyebrow">Decision timeline</div><h1>Payment integration</h1><p>Acceptance and revision ancestry are evaluated independently from source timestamps.</p><div className="timeline"><div className="card"><span className="status">Accepted · effective</span><h3>Use the payment adapter</h3><p>All services must use <code>src/adapters/payment.ts</code>. Legacy settlement remains excepted.</p><div className="muted">ADR-002 · anchored at 68d2… · scope src/services/**</div></div><div className="card"><span className="status warn">Superseded in overlapping scope</span><h3>Direct SDK access</h3><p>Original guidance remains historical evidence, but no longer governs service code.</p></div><div className="card"><span className="status warn">Proposal only</span><h3>“Maybe direct access is simpler”</h3><p>An unaccepted, unmerged comment cannot change policy.</p></div></div></section>}
